<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="com.blacksw.user.mapper.UserMapper">

	<select id="selectTest" parameterType="string" resultType="com.blacksw.user.vo.User">
		SELECT email, password, nickname FROM async_blog.`user` WHERE email = #{email}
	</select>

</mapper>